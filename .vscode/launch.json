{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "notadd",
            "program": "${workspaceFolder}/node_modules/.bin/ts-node",
            "args": [
                "-r",
                "tsconfig-paths/register",
                "${relativeFile}"
            ],
            "cwd": "${workspaceRoot}"
        }
    ]
}